#
# This source file is part of the Stanford Spezi open source project
#
# SPDX-FileCopyrightText: 2026 Stanford University and the project authors (see CONTRIBUTORS.md)
#
# SPDX-License-Identifier: MIT
#

name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: CI-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI
    if: github.event_name != 'workflow_dispatch'
    uses: StanfordBDHG/.github/.github/workflows/swift-package-ci.yml@v2
    with:
      linux_run_tests: true
    secrets: inherit

  publish:
    name: Docker Build and Push
    needs: ci
    if: always() && github.event_name == 'pull_request' && (needs.ci.result == 'success' || needs.ci.result == 'skipped')
    uses: StanfordBDHG/.github/.github/workflows/docker-build-and-push.yml@v2
    with:
      imageName: stanfordspezi/spezistudyplatform-server
    permissions:
      contents: read
      packages: write
    secrets: inherit
